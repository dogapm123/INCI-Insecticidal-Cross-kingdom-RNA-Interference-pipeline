# Regression checks

From the repository root, run `python -m pytest tests` and `node tests/interface_polling.cjs` in the activated Python environment. Install pytest separately; the polling check also needs Node.js.
